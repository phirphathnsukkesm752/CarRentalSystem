# 🚗 Car Rental System (ระบบจัดการเช่ารถยนต์)

ระบบจัดการเช่ารถยนต์แบบเดสก์ท็อปแอปพลิเคชัน พัฒนาด้วยภาษา **Python** โดยใช้ **Tkinter GUI** และระบบจัดการฐานข้อมูล **MySQL** โครงสร้างออกแบบตามหลักการเขียนโปรแกรมเชิงวัตถุ (**Object-Oriented Programming - OOP**) และใช้ Design Patterns ในการจัดสรรโครงสร้างโค้ด

## ✨ ฟีเจอร์หลัก (Features)

### 👤 สำหรับลูกค้า (Customer)

* **ระบบลงทะเบียน & เข้าสู่ระบบ:** สมัครสมาชิกและล็อกอินอย่างปลอดภัย

* **ค้นหาและเลือกชมรถ:** แสดงรายการรถยนต์ ค้นหาตาม ยี่ห้อ/รุ่น/ประเภท

* **ทำรายการเช่ารถ:** คำนวณราคารวมอัตโนมัติตามจำนวนวันที่เช่า

* **ชำระเงินหลายช่องทาง (Polymorphism):** รองรับ เงินสด, บัตรเครดิต และโอนเงินผ่านธนาคาร

* **ประวัติการเช่า:** ตรวจสอบสถานะและประวัติการเช่าของตนเอง

* **ยกเลิกการเช่า:** สามารถยกเลิกรายการเช่าที่ยังใช้งานอยู่ได้

### 👑 สำหรับผู้ดูแลระบบ (Admin)

* **จัดการข้อมูลรถยนต์ (CRUD):** เพิ่ม, แก้ไขข้อมูล, ลบ และอัปเดตสถานะของรถ

* **จัดการรายการเช่า:** ตรวจสอบประวัติการเช่าของลูกค้าทั้งหมด

* **ระบบรับคืนรถยนต์ (Return Vehicle):** คำนวณค่าปรับเกินกำหนดอัตโนมัติ (500 บาท/วัน) พร้อมเลือกช่องทางชำระค่าปรับ

* **ยกเลิกการเ**บสนุนอื่นๆ (System Features)

  * **Auto Initializer:** สร้าง Database, Tables และบัญชี Admin เริ่มต้นให้อัตโนมัติเมื่อเปิดใช้งานครั้งแรก

  * **Data Seeding:** สร้างข้อมูลรถยนต์จำลอง 100 คันเข้าสู่ฐานข้อมูลอัตโนมัติ หากยังไม่มีข้อมูลรถยนต์ในระบบ

  ## **ช่า:** ยกเลิกการเช่าของลูกค้าและคืนสถานะรถยนต์อัตโนมัติ

### ⚙️ ระบบสนั🏗️ โครงสร้างสถาปัตยกรรมซอฟต์แวร์ (OOP & Design Patterns)

1. **Singleton Pattern:** ใช้ในคลาส `DatabaseConnection` เพื่อควบคุมการเชื่อมต่อฐานข้อมูล MySQL เพียงจุดเดียว

2. **DAO Pattern (Data Access Object):** แยกส่วนตรรกะการเข้าถึงฐานข้อมูลออกจาก UI

   * `UserDAO`: จัดการข้อมูลผู้ใช้งานและการ Authentication

   * `VehicleDAO`: จัดการข้อมูลรถยนต์

   * `RentalDAO`: จัดการธุรกรรมการเช่า การรับคืน และการยกเลิก

3. **Polymorphism & Strategy Pattern:** คลาส `PaymentMethod` (Abstract Base Class) พร้อมคลาสย่อย `CashPayment`, `CreditCardPayment`, `BankTransferPayment`

4. **Encapsulation & Inheritance:** คลาสหลัก `User`, `Admin`, `Customer`, `Vehicle` ใช้ Getters และหลักการสืบทอดคุณสมบัติ

5. **Security:** รหัสผ่านถูกเข้ารหัสด้วย **SHA-256 Hash** ก่อนบันทึกลงฐานข้อมูล

## 🛠️ ความต้องการของระบบ (Requirements)

* **Python:** เวอร์ชั่น 3.8 ขึ้นไป

* **MySQL Database Server:** (แนะนำใช้ผ่าน XAMPP, WampServer หรือ MySQL Server)

* **Python Packages:**

  * `mysql-connector-python`

## 🚀 วิธีการติดตั้งและเริ่มใช้งาน (Getting Started)

### 1. เตรียมความพร้อมของฐานข้อมูล

1. เปิดใช้งาน **MySQL Service** (เช่น เปิด MySQL บน Control Panel ของ XAMPP)

2. ตรวจสอบการตั้งค่าในไฟล์ `CarRentalSystem.py` ที่คลาส `DatabaseConnection._config`:

   ```
   _config = {
       'host': 'localhost',
       'user': 'root',
       'password': '',  # ใส่รหัสผ่าน MySQL ของคุณ (ถ้ามี)
       'database': 'car_rental_db',
       'port': 3306,
   }
   
   ```

### 2. ติดตั้ง Dependencies

เปิด Terminal / Command Prompt แล้วรันคำสั่ง:

```
pip install mysql-connector-python

```

### 3. เริ่มรันโปรแกรม

```
python CarRentalSystem.py

```

> **หมายเหตุ:** โปรแกรมจะทำการสร้างฐานข้อมูล `car_rental_db`, ตารางต่างๆ, สุ่มข้อมูลรถ 100 คัน และสร้างบัญชี Admin ให้อัตโนมัติเมื่อรันครั้งแรก

## 🔐 บัญชีเข้าใช้งานเริ่มต้น (Default Credentials)

* **สิทธิ์ Administrator:**

  * **Username:** `admin`

  * **Password:** `admin123`

* **สิทธิ์ Customer:**

  * สามารถกดปุ่ม **"ลงทะเบียนผู้ใช้งานใหม่ (เฉพาะลูกค้า)"** หน้าแรกเพื่อสมัครสมาชิกได้ทันที

## 🗄️ โครงสร้างฐานข้อมูล (Database Schema)

| ตาราง (Table) | รายละเอียด (Description) | 
 | ----- | ----- | 
| `users` | เก็บข้อมูลผู้ใช้งาน รหัสผ่าน (SHA-256) และสิทธิ์ (`ADMIN`, `CUSTOMER`) | 
| `customers` | เก็บรายละเอียดข้อมูลส่วนตัวของลูกค้า (ชื่อ, เบอร์โทร, ใบขับขี่ ฯลฯ) | 
| `vehicles` | เก็บข้อมูลรถยนต์ (ยี่ห้อ, รุ่น, ทะเบียน, ราคา/วัน, สถานะ) | 
| `rentals` | เก็บข้อมูลการทำรายการเช่า (วันเริ่ม, กำหนดคืน, วันคืนจริง, ค่าปรับ, สถานะ) | 
| `payments` | เก็บประวัติการชำระเงิน (ช่องทางชำระเงิน, ยอดชำระ, เวลา) | 
